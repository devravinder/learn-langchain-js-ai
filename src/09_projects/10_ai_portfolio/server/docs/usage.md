# Usage

1. run `pnpm run dev`
2. visit `http://localhost:3001/openapi`
3. from UI:
   1. run ui: `pnpm run dev` from client app & visit `http://localhost:5173`
   2. ask questions like `I want to build AI Chat app for IOS mobile, what are the talent gaps, suggest me some developers`


